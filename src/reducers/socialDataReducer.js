// defaults for social data
const defaultState = {
  lang:'en-us'
};
// the reducers for social data
const socialDataReducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
        return state;
  }
};

export default socialDataReducer;
